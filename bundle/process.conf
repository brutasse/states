{% set bundle_root = "/home/" + pillar['user'] + "/bundles/" + config['http_host'] %}[program:{{ process }}]
command = envdir /etc/{{ config['http_host'] }}.d {{ command }}
directory = {{ bundle_root }}
user = {{ pillar['user'] }}
autostart = true
autorestart = true
redirect_stderr = true
stdout_logfile = {{ bundle_root }}/log/stdout.log
stderr_logfile = {{ bundle_root }}/log/stderr.log
